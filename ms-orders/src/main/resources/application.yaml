spring:
  application:
    name: ms-orders
  datasource:
    url: jdbc:mysql://localhost:3306/orders
    username: root
    password: admin
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
server:
  port: 8083